<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.k210.licj.k210.mapper.FilterMapper">

    <select id="selectHavePath" resultType="java.lang.Integer" parameterType="java.lang.String">
        select count(*) from white_path where path = #{requestPath}
    </select>


</mapper>
